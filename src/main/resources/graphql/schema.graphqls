type Player {
    id: ID!
    nickname: String!
    alliance: Alliance
}

type Alliance {
    id: ID!
    name: String!
    players: [Player!]!
}

type Query {
    currentPlayer: Player
    player(playerId: ID!): Player
    alliances: [Alliance!]!
}
